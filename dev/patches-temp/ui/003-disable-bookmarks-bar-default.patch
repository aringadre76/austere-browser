--- a/chrome/browser/prefs/browser_prefs.cc
+++ b/chrome/browser/prefs/browser_prefs.cc
@@ -1229,6 +1229,7 @@ void RegisterBrowserUserPrefs(user_prefs::PrefRegistrySyncable* registry) {
   registry->RegisterBooleanPref(prefs::kShowBookmarkBar, false);
+  // Austere: Bookmarks bar disabled by default for cleaner UI
   registry->RegisterBooleanPref(prefs::kShowAppsShortcutInBookmarkBar, true);
