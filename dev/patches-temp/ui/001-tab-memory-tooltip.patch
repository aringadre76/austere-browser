--- a/chrome/browser/ui/views/tabs/tab.cc
+++ b/chrome/browser/ui/views/tabs/tab.cc
@@ -50,6 +50,8 @@
 #include "chrome/browser/ui/views/tabs/tab_close_button.h"
 #include "chrome/browser/ui/views/tabs/tab_hover_card_bubble_view.h"
 #include "chrome/browser/ui/views/tabs/tab_slot_controller.h"
+#include "chrome/browser/ui/performance_controls/tab_resource_usage_tab_helper.h"
+#include "base/byte_count.h"
+#include "ui/base/text/bytes_formatting.h"
 #include "chrome/common/chrome_features.h"
 #include "chrome/grit/generated_resources.h"
 #include "components/grit/components_scaled_resources.h"
@@ -767,9 +769,35 @@

 std::u16string Tab::GetRenderedTooltipText(const gfx::Point& p) const {
-  if (base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII("tab-hover-cards") == "tooltip")
-    return GetTooltipText(data_.title, GetAlertStateToShow(data_.alert_state));
+  std::u16string tooltip = data_.title;
+
+  if (data_.tab_resource_usage &&
+      data_.tab_resource_usage->memory_usage().is_positive()) {
+    const base::ByteCount memory_used =
+        data_.tab_resource_usage->memory_usage();
+    const bool is_high_memory_usage =
+        data_.tab_resource_usage->is_high_memory_usage();
+
+    if (!tooltip.empty()) {
+      tooltip.append(1, '\n');
+    }
+
+    const int message_id = is_high_memory_usage ? IDS_TAB_AX_HIGH_MEMORY_USAGE
+                                                : IDS_TAB_AX_MEMORY_USAGE;
+    tooltip.append(l10n_util::GetStringFUTF16(message_id, data_.title,
+                                             ui::FormatBytes(memory_used)));
+  } else if (data_.should_show_discard_status &&
+             data_.discarded_memory_savings.is_positive()) {
+    if (!tooltip.empty()) {
+      tooltip.append(1, '\n');
+    }
+    tooltip.append(l10n_util::GetStringFUTF16(
+        IDS_TAB_AX_MEMORY_SAVINGS, data_.title,
+        ui::FormatBytes(data_.discarded_memory_savings)));
+  }
+
+  std::optional<tabs::TabAlert> alert_state = GetAlertStateToShow(data_.alert_state);
+  if (alert_state) {
+    if (!tooltip.empty()) {
+      tooltip.append(1, '\n');
+    }
+    tooltip.append(
+        tabs::TabAlertController::GetTabAlertStateText(alert_state.value()));
+  }
+
+  if (!tooltip.empty()) {
+    return tooltip;
+  }
   // Tab hover cards replace tooltips for tabs.
-  return std::u16string();
+  return std::u16string();
 }
