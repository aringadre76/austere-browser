--- a/components/performance_manager/user_tuning/prefs.cc
+++ b/components/performance_manager/user_tuning/prefs.cc
@@ -1,3 +1,5 @@
+// Austere Browser: Memory saver with customizable levels
+// Default: Super Aggressive mode for maximum memory savings (user can change in settings)
 // Copyright 2022 The Chromium Authors
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
@@ -20,13 +22,18 @@ namespace performance_manager::user_tuning::prefs {

 void RegisterLocalStatePrefs(PrefRegistrySimple* registry) {
-  registry->RegisterBooleanPref(kMemorySaverModeEnabled, false);
+  // Austere: Enable memory saver by default, but allow user customization
+  registry->RegisterBooleanPref(kMemorySaverModeEnabled, true);
   registry->RegisterIntegerPref(
       kMemorySaverModeTimeBeforeDiscardInMinutes,
-      kDefaultMemorySaverModeTimeBeforeDiscardInMinutes);
+      2);  // Austere: 2 minutes default
   registry->RegisterIntegerPref(
-      kMemorySaverModeState, static_cast<int>(MemorySaverModeState::kDisabled));
+      kMemorySaverModeState, static_cast<int>(MemorySaverModeState::kEnabled));  // Austere: Enabled by default
   registry->RegisterIntegerPref(
       kMemorySaverModeAggressiveness,
-      static_cast<int>(MemorySaverModeAggressiveness::kMedium));
+      // Austere: Default to Super Aggressive (4) for maximum memory savings
+      // Users can change this in settings: None, Light, Medium, Aggressive, Super Aggressive
+      static_cast<int>(MemorySaverModeAggressiveness::kSuperAggressive));
   registry->RegisterIntegerPref(
       kBatterySaverModeState,
       static_cast<int>(BatterySaverModeState::kEnabledBelowThreshold));
