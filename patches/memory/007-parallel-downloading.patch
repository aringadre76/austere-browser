--- a/components/download/public/common/download_features.cc
+++ b/components/download/public/common/download_features.cc
@@ -1,3 +1,5 @@
+// Austere Browser: Enable parallel downloading on all platforms
+// for faster download speeds
 // Copyright 2018 The Chromium Authors
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
@@ -9,11 +11,8 @@
 namespace download {
 namespace features {

+// Austere: Enable parallel downloading on all platforms (not just Android)
 BASE_FEATURE(kParallelDownloading,
-#if BUILDFLAG(IS_ANDROID)
-             base::FEATURE_ENABLED_BY_DEFAULT
-#else
-             base::FEATURE_DISABLED_BY_DEFAULT
-#endif
+             base::FEATURE_ENABLED_BY_DEFAULT
 );

 #if BUILDFLAG(IS_WIN) || BUILDFLAG(IS_MAC)
