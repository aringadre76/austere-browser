--- a/chrome/browser/performance_manager/policies/memory_saver_mode_policy.cc
+++ b/chrome/browser/performance_manager/policies/memory_saver_mode_policy.cc
@@ -1,3 +1,5 @@
+// Austere Browser: Aggressive tab discard times
+// Reduced from hours to seconds for maximum memory savings
 // Copyright 2022 The Chromium Authors
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
@@ -203,11 +205,14 @@ void MemorySaverModePolicy::DiscardPageTimerCallback(
 base::TimeDelta MemorySaverModePolicy::GetTimeBeforeDiscardForCurrentMode()
     const {
   switch (mode_) {
     case MemorySaverModeAggressiveness::kConservative:
-      return base::Hours(6);
+      // Austere: 60 seconds instead of 6 hours
+      return base::Seconds(60);
     case MemorySaverModeAggressiveness::kMedium:
-      return base::Hours(4);
+      // Austere: 45 seconds instead of 4 hours
+      return base::Seconds(45);
     case MemorySaverModeAggressiveness::kAggressive:
-      return base::Hours(2);
+      // Austere: 30 seconds instead of 2 hours
+      return base::Seconds(30);
   }
   NOTREACHED();
 }
